const insert = (array, rightIndex, value) => {
  // do something
};

const array = [3, 5, 7, 11, 15, 2, 9, 6];

insert(array, 4, 2);
console.log(`Array after inserting 2: ${array}`);

insert(array, 5, 9);
console.log(`Array after inserting 9: ${array}`);
